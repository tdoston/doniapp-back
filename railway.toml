# doniapp-back — Railpack (default). NIXPACKS + `pip` ishlatilmaydi (pip: command not found).
# Build: DATABASE_URL kerak. Start: bootstrap + migrate + gunicorn.

[build]
buildCommand = "chmod +x scripts/railway_build.sh scripts/railway_start.sh && bash scripts/railway_build.sh"

[deploy]
startCommand = "bash scripts/railway_start.sh"
