# doniapp-back — Railpack. Build: collectstatic. Start: gunicorn + fon DB.

[build]
buildCommand = "chmod +x scripts/railway_*.sh && bash scripts/railway_build.sh"

[deploy]
startCommand = "bash scripts/railway_start.sh"
