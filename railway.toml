# doniapp-back — Railpack default build; faqat start override

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 60
startCommand = "chmod +x scripts/railway.sh && bash scripts/railway.sh start"
