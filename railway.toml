# doniapp-back — repo root = django_backend fayllari

[build]
buildCommand = "chmod +x scripts/railway.sh && bash scripts/railway.sh build"

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 60
startCommand = "chmod +x scripts/railway.sh && bash scripts/railway.sh start"
