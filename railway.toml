# doniapp-back — Root Directory: django_backend

[build]
buildCommand = "chmod +x scripts/railway.sh && ./scripts/railway.sh build"

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 120
startCommand = "./scripts/railway.sh start"
